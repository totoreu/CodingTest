#include <iostream>

int main() 
{
    int N = 0, K = 0;
    std::cin >> N >> K;

    int A = 0, B = 0;
    for (int i = 0; i < N; ++i)
    {
        std::cin >> A >> B;
    }
    std::cout << "비와이";
    return 0;
}